name: majid
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  data_table_2: ^2.6.0
  fl_chart: ^1.0.0
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - "assets/logo.png"
    - "assets/avatar.jpg"

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"
  app_name: "Herar Neurology"